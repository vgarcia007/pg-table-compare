version: "3.9"

services:
  db-change-detector:
    image: python:3.11-slim
    container_name: db-change-detector
    working_dir: /app
    volumes:
      - ./:/app:rw
    env_file:
      - .env
    command: >
      sh -c "tail -f /dev/null"
